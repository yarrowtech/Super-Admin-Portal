// backend/controllers/legalDocument.controller.js const mongoose = require('mongoose'); const LegalDocument = require('../models/law/LegalDocument'); const LegalDocumentVersion = require('../models/law/LegalDocumentVersion'); const LegalAuditLog = require('../models/law/LegalAuditLog'); const logger = require('../utils/logger');  // ── Helpers ──────────────────────────────────────────────────────────────────  const VALID_TYPES = new Set(['Contract', 'Agreement', 'Policy', 'NDA', 'Compliance', 'IP', 'Dispute', 'Other']); const VALID_STATUSES = new Set(['Draft', 'Pending', 'Approved', 'Rejected']);  const normalizePagination = (value, fallback, max) => {   const parsed = parseInt(value, 10);   if (Number.isNaN(parsed)) return fallback;   return Math.min(Math.max(parsed, 1), max); };  const ensureObjectId = (value, label) => {   if (!value || !mongoose.Types.ObjectId.isValid(value)) {     const error = new Error(`Valid ${label} is required`);     error.statusCode = 400;     throw error;   } };  const getActorContext = (req) => ({   actorId: req.user?._id || req.user?.id,   role: req.user?.role || 'unknown',   name:     [req.user?.firstName, req.user?.lastName].filter(Boolean).join(' ').trim() ||     req.user?.name ||     req.user?.email ||     'Unknown',   ipAddress:     req.headers['x-forwarded-for']?.split(',')[0]?.trim() ||     req.ip ||     req.socket?.remoteAddress ||     '', });  const emitLegalEvent = (req, event, payload) => {   const io = req.app?.get('io');   if (io) io.emit(event, payload); };  const createAuditLog = async (req, documentId, action, remarks = '', metadata = {}) => {   const actor = getActorContext(req);   if (!actor.actorId) return;    await LegalAuditLog.create({     documentId,     action,     performedBy: actor.actorId,     role: actor.role,     remarks,     metadata,     timestamp: new Date(),   }); };  const requirePuppeteer = () => {   try {     return require('puppeteer');   } catch (error) {     const err = new Error('PDF generation requires puppeteer to be installed');     err.statusCode = 500;     throw err;   } };  const buildPdfHtml = (doc) => {   const title = doc.title || 'Legal Document';   const content = doc.latestContent || '<p>No content available.</p>';   const remarks = doc.ceoRemarks     ? `<section class="meta-block"><h3>CEO Remarks</h3><p>${doc.ceoRemarks}</p></section>`     : '';    return `<!doctype html>   <html>     <head>       <meta charset="utf-8" />       <title>${title}</title>       <style>         body { font-family: Arial, sans-serif; color: #111827; margin: 0; padding: 32px; }         .page { max-width: 900px; margin: 0 auto; }         header { border-bottom: 2px solid #111827; margin-bottom: 24px; padding-bottom: 16px; }         h1 { margin: 0 0 8px; font-size: 28px; }         .meta { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px 24px; font-size: 13px; color: #374151; }         .meta strong { color: #111827; }         .content { font-size: 14px; line-height: 1.6; word-break: break-word; }         .meta-block { margin-top: 24px; padding: 16px; border: 1px solid #d1d5db; border-radius: 10px; background: #f9fafb; }         .meta-block h3 { margin: 0 0 8px; font-size: 15px; }         @media print { body { padding: 0; } .page { max-width: none; } }       </style>     </head>     <body>       <div class="page">         <header>           <h1>${title}</h1>           <div class="meta">             <div><strong>Type:</strong> ${doc.type || 'Other'}</div>             <div><strong>Version:</strong> ${doc.currentVersion || 'v1.0'}</div>             <div><strong>Status:</strong> ${doc.status || 'Draft'}</div>             <div><strong>Project:</strong> ${doc.projectName || 'N/A'}</div>             <div><strong>Owner:</strong> ${doc.owner || 'N/A'}</div>             <div><strong>Updated:</strong> ${doc.updatedAt ? new Date(doc.updatedAt).toISOString() : 'N/A'}</div>           </div>         </header>         <main class="content">${content}</main>         ${remarks}       </div>     </body>   </html>`; };  /** Bump version: minor increment by default, major on re-submit after rejection */ const bumpVersion = (doc, major = false) => {   if (major) {     return {       versionMajor: doc.versionMajor + 1,       versionMinor: 0,       currentVersion: `v${doc.versionMajor + 1}.0`,     };   }   return {     versionMajor: doc.versionMajor,     versionMinor: doc.versionMinor + 1,     currentVersion: `v${doc.versionMajor}.${doc.versionMinor + 1}`,   }; };  /** Create a version snapshot */ const createVersionSnapshot = async (doc, req, summary) => {   const actor = getActorContext(req);   const content = doc.latestContent && String(doc.latestContent).trim() ? doc.latestContent : '<p><br></p>';   return LegalDocumentVersion.create({     documentId: doc._id,     version: doc.currentVersion,     content,     editedBy: actor.actorId,     editedByName: actor.name,     changeSummary: summary || 'Document saved',     statusAtTime: doc.status,   }); };  // ── LAW PORTAL ────────────────────────────────────────────────────────────────  /**  * POST /api/legal/create  * Create a new legal document (LAW role)  */ exports.create = async (req, res) => {   try {     const { title, type, projectId, projectName, content, priority, tags, owner } = req.body;     if (!title || !String(title).trim()) {       return res.status(400).json({ success: false, error: 'Title is required' });     }     if (projectId) ensureObjectId(projectId, 'projectId');      const actor = getActorContext(req);      const doc = await LegalDocument.create({       title: String(title).trim(),       type: VALID_TYPES.has(type) ? type : 'Other',       projectId: projectId || undefined,       projectName: projectName ? String(projectName).trim() : '',       owner: owner ? String(owner).trim() : actor.name,       latestContent: content || '',       currentVersion: 'v1.0',       versionMajor: 1,       versionMinor: 0,       status: 'Draft',       isLocked: false,       isPublished: false,       createdBy: actor.actorId,       createdByName: actor.name,       priority: priority || 'Medium',       tags: Array.isArray(tags) ? tags : [],     });      // Initial version snapshot     await LegalDocumentVersion.create({       documentId: doc._id,       version: 'v1.0',       content: content && String(content).trim() ? content : '<p><br></p>',       editedBy: actor.actorId,       editedByName: actor.name,       changeSummary: 'Document created',       statusAtTime: 'Draft',     });      await createAuditLog(req, doc._id, 'CREATE', 'Document created', {       title: doc.title,       type: doc.type,       version: doc.currentVersion,       projectId: doc.projectId || null,     });      emitLegalEvent(req, 'legal:document:created', {       documentId: doc._id,       title: doc.title,       createdBy: actor.name,       status: doc.status,       version: doc.currentVersion,     });      return res.status(201).json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.create failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * PUT /api/legal/:id/auto-save  * Auto-save (just updates content, no version snapshot, no version bump)  */ exports.autoSave = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.isLocked) return res.status(403).json({ success: false, error: 'Document is locked' });      if (req.body.content !== undefined) doc.latestContent = req.body.content;     if (req.body.title) doc.title = String(req.body.title).trim();     if (req.body.type) doc.type = VALID_TYPES.has(req.body.type) ? req.body.type : doc.type;     if (req.body.projectId) {       ensureObjectId(req.body.projectId, 'projectId');       doc.projectId = req.body.projectId;     }     if (req.body.projectName !== undefined) doc.projectName = String(req.body.projectName || '').trim();     if (req.body.owner !== undefined) doc.owner = String(req.body.owner || '').trim();     await doc.save();      return res.json({ success: true, data: { savedAt: doc.updatedAt } });   } catch (err) {     logger.error({ err }, 'legalDocument.autoSave failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * PUT /api/legal/:id/save-draft  * Manual save — bumps minor version, creates snapshot  */ exports.saveDraft = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.isLocked) return res.status(403).json({ success: false, error: 'Document is locked' });      const actor = getActorContext(req);      const { title, type, content, projectId, projectName, owner, priority, tags, changeSummary } = req.body;      // Update fields     if (title !== undefined) doc.title = String(title).trim();     if (type !== undefined) doc.type = VALID_TYPES.has(type) ? type : doc.type;     if (content !== undefined) doc.latestContent = content;     if (projectId !== undefined) {       ensureObjectId(projectId, 'projectId');       doc.projectId = projectId;     }     if (projectName !== undefined) doc.projectName = String(projectName || '').trim();     if (owner !== undefined) doc.owner = String(owner || '').trim();     if (priority !== undefined) doc.priority = priority;     if (tags !== undefined) doc.tags = Array.isArray(tags) ? tags : [];      // Bump minor version     const bump = bumpVersion(doc, false);     doc.versionMajor = bump.versionMajor;     doc.versionMinor = bump.versionMinor;     doc.currentVersion = bump.currentVersion;      await doc.save();      // Create version snapshot     await createVersionSnapshot(doc, req, changeSummary || 'Draft saved');     await createAuditLog(req, doc._id, 'UPDATE', changeSummary || 'Draft saved', {       version: doc.currentVersion,       status: doc.status,     });      emitLegalEvent(req, 'legal:document:updated', {       documentId: doc._id,       title: doc.title,       updatedBy: actor.name,       version: doc.currentVersion,       status: doc.status,     });      return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.saveDraft failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * POST /api/legal/:id/submit  * Submit to CEO for approval  */ exports.submit = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.isLocked) return res.status(403).json({ success: false, error: 'Document is locked (already approved)' });     if (!['Draft', 'Rejected'].includes(doc.status)) {       return res.status(400).json({ success: false, error: `Cannot submit a document with status: ${doc.status}` });     }     if (!doc.latestContent || !String(doc.latestContent).trim()) {       return res.status(400).json({ success: false, error: 'Content required before submission' });     }      const actor = getActorContext(req);      // If re-submitting after rejection → major version bump     const wasRejected = doc.status === 'Rejected';     const bump = bumpVersion(doc, wasRejected);     doc.versionMajor = bump.versionMajor;     doc.versionMinor = bump.versionMinor;     doc.currentVersion = bump.currentVersion;      doc.status = 'Pending';     doc.submittedAt = new Date();     doc.approvedAt = null;     doc.rejectedAt = null;     doc.ceoRemarks = ''; // Clear previous remarks on re-submit      // Update content if provided     if (req.body.content !== undefined) doc.latestContent = req.body.content;      await doc.save();      // Version snapshot     await createVersionSnapshot(       doc,       req,       wasRejected ? 'Re-submitted after rejection' : 'Submitted to CEO for approval'     );     await createAuditLog(       req,       doc._id,       'SUBMIT',       wasRejected ? 'Re-submitted after rejection' : 'Submitted to CEO for approval',       {         version: doc.currentVersion,         submittedAt: doc.submittedAt,       }     );      emitLegalEvent(req, 'legal:document:submitted', {       documentId: doc._id,       title: doc.title,       submittedBy: actor.name,       version: doc.currentVersion,       status: doc.status,     });      return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.submit failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * GET /api/legal/my-documents  * LAW user's own documents  */ exports.myDocuments = async (req, res) => {   try {     const { status, type, page = 1, limit = 50, search } = req.query;     const filter = { createdBy: req.user._id };     if (status && VALID_STATUSES.has(status)) filter.status = status;     if (type && VALID_TYPES.has(type)) filter.type = type;     if (search) filter.$text = { $search: search };      const normalizedPage = normalizePagination(page, 1, 1000000);     const normalizedLimit = normalizePagination(limit, 50, 100);     const skip = (normalizedPage - 1) * normalizedLimit;     const [items, total] = await Promise.all([       LegalDocument.find(filter).sort({ updatedAt: -1 }).skip(skip).limit(normalizedLimit).lean(),       LegalDocument.countDocuments(filter),     ]);      return res.json({       success: true,       data: { items, total, page: normalizedPage, limit: normalizedLimit },     });   } catch (err) {     logger.error({ err }, 'legalDocument.myDocuments failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * GET /api/legal/:id  * Get single document  */ exports.getById = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id).lean();     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.getById failed');     return res.status(500).json({ success: false, error: err.message });   } };  // ── CEO PORTAL ────────────────────────────────────────────────────────────────  /**  * GET /api/legal/pending  * CEO: Get all pending documents  */ exports.getPending = async (req, res) => {   try {     const { page = 1, limit = 50, type, priority } = req.query;     const filter = { status: 'Pending' };     if (type && VALID_TYPES.has(type)) filter.type = type;     if (priority) filter.priority = priority;      const normalizedPage = normalizePagination(page, 1, 1000000);     const normalizedLimit = normalizePagination(limit, 50, 100);     const skip = (normalizedPage - 1) * normalizedLimit;     const [items, total] = await Promise.all([       LegalDocument.find(filter).sort({ submittedAt: -1 }).skip(skip).limit(normalizedLimit).lean(),       LegalDocument.countDocuments(filter),     ]);      return res.json({       success: true,       data: { items, total, page: normalizedPage, limit: normalizedLimit },     });   } catch (err) {     logger.error({ err }, 'legalDocument.getPending failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * POST /api/legal/:id/approve  * CEO: Approve a document  */ exports.approve = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.status !== 'Pending') {       return res.status(400).json({ success: false, error: 'Only pending documents can be approved' });     }      const actor = getActorContext(req);      doc.status = 'Approved';     doc.isLocked = true;     doc.isPublished = true;     doc.approvedBy = actor.actorId;     doc.approvedAt = new Date();     doc.rejectedAt = null;     doc.ceoRemarks = req.body.remarks || '';      await doc.save();     await createAuditLog(req, doc._id, 'APPROVE', req.body.remarks || 'Approved', {       approvedAt: doc.approvedAt,       version: doc.currentVersion,     });      emitLegalEvent(req, 'legal:document:approved', {       documentId: doc._id,       title: doc.title,       approvedBy: actor.name,       version: doc.currentVersion,       remarks: doc.ceoRemarks,     });      return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.approve failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * POST /api/legal/:id/reject  * CEO: Reject a document (remark is mandatory)  */ exports.reject = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const { remarks } = req.body;     if (!remarks || !remarks.trim()) {       return res.status(400).json({ success: false, error: 'Rejection remarks are required' });     }      const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.status !== 'Pending') {       return res.status(400).json({ success: false, error: 'Only pending documents can be rejected' });     }      const actor = getActorContext(req);     doc.status = 'Rejected';     doc.ceoRemarks = remarks;     doc.rejectedAt = new Date();     doc.approvedAt = null;     doc.isLocked = false;     doc.isPublished = false;      await doc.save();     await createAuditLog(req, doc._id, 'REJECT', remarks, {       rejectedAt: doc.rejectedAt,       version: doc.currentVersion,     });      emitLegalEvent(req, 'legal:document:rejected', {       documentId: doc._id,       title: doc.title,       rejectedBy: actor.name,       version: doc.currentVersion,       remarks,     });      return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.reject failed');     return res.status(500).json({ success: false, error: err.message });   } };  // ── ADMIN / LSW PORTAL ────────────────────────────────────────────────────────  /**  * GET /api/legal/approved  * Admin/LSW: Get all approved/published documents  */ exports.getApproved = async (req, res) => {   try {     const { type, projectId, page = 1, limit = 50, search } = req.query;     const filter = { status: 'Approved', isPublished: true };     if (type && VALID_TYPES.has(type)) filter.type = type;     if (projectId) {       ensureObjectId(projectId, 'projectId');       filter.projectId = projectId;     }     if (search) filter.$text = { $search: search };      const normalizedPage = normalizePagination(page, 1, 1000000);     const normalizedLimit = normalizePagination(limit, 50, 100);     const skip = (normalizedPage - 1) * normalizedLimit;     const [items, total] = await Promise.all([       LegalDocument.find(filter)         .select('-latestContent') // omit heavy content from list view         .sort({ approvedAt: -1 })         .skip(skip)         .limit(normalizedLimit)         .lean(),       LegalDocument.countDocuments(filter),     ]);      return res.json({       success: true,       data: { items, total, page: normalizedPage, limit: normalizedLimit },     });   } catch (err) {     logger.error({ err }, 'legalDocument.getApproved failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * GET /api/legal/all  * Admin: All documents (any status) for oversight  */ exports.getAll = async (req, res) => {   try {     const { status, type, projectId, page = 1, limit = 50, search } = req.query;     const filter = {};     if (status && VALID_STATUSES.has(status)) filter.status = status;     if (type && VALID_TYPES.has(type)) filter.type = type;     if (projectId) {       ensureObjectId(projectId, 'projectId');       filter.projectId = projectId;     }     if (search) filter.$text = { $search: search };      const normalizedPage = normalizePagination(page, 1, 1000000);     const normalizedLimit = normalizePagination(limit, 50, 100);     const skip = (normalizedPage - 1) * normalizedLimit;     const [items, total] = await Promise.all([       LegalDocument.find(filter)         .select('-latestContent')         .sort({ updatedAt: -1 })         .skip(skip)         .limit(normalizedLimit)         .lean(),       LegalDocument.countDocuments(filter),     ]);      return res.json({       success: true,       data: { items, total, page: normalizedPage, limit: normalizedLimit },     });   } catch (err) {     logger.error({ err }, 'legalDocument.getAll failed');     return res.status(500).json({ success: false, error: err.message });   } };  // ── VERSION CONTROL ───────────────────────────────────────────────────────────  /**  * GET /api/legal/:id/versions  * Get version history for a document  */ exports.getVersions = async (req, res) => {   try {     const versions = await LegalDocumentVersion.find({ documentId: req.params.id })       .sort({ createdAt: -1 })       .lean();     return res.json({ success: true, data: versions });   } catch (err) {     logger.error({ err }, 'legalDocument.getVersions failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * GET /api/legal/version/:versionId  * Get a single version (for preview)  */ exports.getVersionById = async (req, res) => {   try {     const version = await LegalDocumentVersion.findById(req.params.versionId).lean();     if (!version) return res.status(404).json({ success: false, error: 'Version not found' });     return res.json({ success: true, data: version });   } catch (err) {     logger.error({ err }, 'legalDocument.getVersionById failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * POST /api/legal/:id/restore/:versionId  * Restore document to a previous version  */ exports.restoreVersion = async (req, res) => {   try {     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.isLocked) return res.status(403).json({ success: false, error: 'Document is locked and cannot be restored' });      const version = await LegalDocumentVersion.findById(req.params.versionId);     if (!version) return res.status(404).json({ success: false, error: 'Version not found' });     if (String(version.documentId) !== String(doc._id)) {       return res.status(400).json({ success: false, error: 'Version does not belong to this document' });     }      doc.latestContent = version.content;      // Bump minor version for the restore action     const bump = bumpVersion(doc, false);     doc.versionMajor = bump.versionMajor;     doc.versionMinor = bump.versionMinor;     doc.currentVersion = bump.currentVersion;      await doc.save();      // Snapshot the restored state     await createVersionSnapshot(doc, req, `Restored from ${version.version}`);     await createAuditLog(req, doc._id, 'RESTORE', `Restored from ${version.version}`, {       restoredFrom: version._id,       version: doc.currentVersion,     });      emitLegalEvent(req, 'legal:document:restored', {       documentId: doc._id,       title: doc.title,       restoredFrom: version.version,       version: doc.currentVersion,     });      return res.json({ success: true, data: doc });   } catch (err) {     logger.error({ err }, 'legalDocument.restoreVersion failed');     return res.status(500).json({ success: false, error: err.message });   } };  /**  * DELETE /api/legal/:id  * Admin only: delete a document (must be Draft or Rejected)  */ exports.deleteDocument = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id);     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });     if (doc.isLocked) return res.status(403).json({ success: false, error: 'Cannot delete an approved document' });      await createAuditLog(req, doc._id, 'DELETE', 'Document deleted', {       status: doc.status,       version: doc.currentVersion,     });     await LegalDocumentVersion.deleteMany({ documentId: doc._id });     await doc.deleteOne();      return res.json({ success: true, message: 'Document deleted' });   } catch (err) {     logger.error({ err }, 'legalDocument.delete failed');     return res.status(500).json({ success: false, error: err.message });   } };  exports.generatePdf = async (req, res) => {   try {     ensureObjectId(req.params.id, 'document id');     const doc = await LegalDocument.findById(req.params.id).lean();     if (!doc) return res.status(404).json({ success: false, error: 'Document not found' });      const puppeteer = requirePuppeteer();     const browser = await puppeteer.launch({       headless: 'new',       args: ['--no-sandbox', '--disable-setuid-sandbox'],     });      try {       const page = await browser.newPage();       await page.setContent(buildPdfHtml(doc), { waitUntil: 'load' });       const pdfBuffer = await page.pdf({         format: 'A4',         printBackground: true,         margin: { top: '20mm', right: '16mm', bottom: '20mm', left: '16mm' },       });        await createAuditLog(req, doc._id, 'PDF_GENERATE', 'PDF generated', {         version: doc.currentVersion,         status: doc.status,       });        const safeTitle =         String(doc.title || 'legal-document')           .trim()           .toLowerCase()           .replace(/[^a-z0-9]+/g, '-')           .replace(/^-+|-+$/g, '') || 'legal-document';        res.setHeader('Content-Type', 'application/pdf');       res.setHeader(         'Content-Disposition',         `attachment; filename="${safeTitle}-${doc.currentVersion || 'v1-0'}.pdf"`       );       return res.send(pdfBuffer);     } finally {       await browser.close();     }   } catch (err) {     logger.error({ err }, 'legalDocument.generatePdf failed');     return res.status(err.statusCode || 500).json({ success: false, error: err.message });   } };