# Pages

Use this folder for new full-page screens.

Recommended simple structure:

```txt
pages/
  auth/
  dashboard/
  users/
  roles/
  profile/
  errors/
```

Existing project screens currently live in `components/` and can be moved here gradually.
