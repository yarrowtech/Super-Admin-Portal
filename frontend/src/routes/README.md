# Routes

This folder keeps route setup simple:

- `AppRoutes.jsx` contains all application routes.
- `ProtectedRoute.jsx` checks login.
- `RoleRoute.jsx` checks allowed roles.
- `routeHelpers.js` keeps small routing helper functions.

